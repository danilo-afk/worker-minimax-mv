# ComfyUI API templates

Only sanitized, independently adapted API-format templates belong here.

Do not commit original downloaded workflow archives or UI exports from paid/private sources.

`music3.json` is an independent API adaptation of the documented Music 3
pipeline. Runtime values are injected by `handler.py`; node IDs are internal and
must not leak into the platform provider contract.
