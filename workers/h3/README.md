# MiniMax H3 worker

Generates one reference-to-video clip per audio segment.

Planned inputs include a character sheet, an audio segment, a scene prompt, aspect ratio, duration and seed. Upscaling and frame interpolation remain optional post-processing stages.
