# Krea 2 character-sheet worker

Generates a consistent multi-view reference sheet for later MiniMax H3 scenes.

The worker will expose a prompt-oriented contract and keep Krea-specific ComfyUI nodes and LoRA paths internal.
